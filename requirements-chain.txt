# Optional: only needed if GUARDIAN_WALLET_PROVIDER=rpc (real on-chain data
# via direct JSON-RPC). The mock provider and the Blockscout/DexScreener
# providers only need requirements.txt - this stays separate so the base
# install remains small if you don't need direct RPC access.
#
#   pip install -r requirements.txt -r requirements-chain.txt
web3==7.4.0
